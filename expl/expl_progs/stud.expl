type
student{
str name;
int marks;
}
endtype

int main(){
decl
    int temp;
    student j;
enddecl
begin
    temp=initialize();
    j=alloc();
    j.name="John";
    j.marks=89;
    temp=exposcall("Write",-2,j.name);
    temp=exposcall("Write",-2,j.marks);
    return 0;
end
}